# Phase 1-2: LangGraph agent basics + multi-agent coordination
langgraph
langchain-core
python-dotenv

# LLM providers, tried in this order — see agents/llm.py
langchain-groq
langchain-openai
langchain-anthropic

pytest

# Persistence (Neon Postgres) — see CLAUDE.md "Persistence (Neon Postgres)".
# Wired into the app (game/run_game.py, backend/) as of Phase 5.
sqlalchemy
psycopg[binary]
alembic

# Phase 6: DeepEval scoring + annotation — see eval/.
deepeval

# Phase 3: map/geo generation — see map_data/generate_map.py.
# Only generate_map.py imports these; the game itself just reads the
# committed provinces.geojson, no geo libraries needed at runtime.
geopandas
shapely
pyogrio
h3

# Phase 5: FastAPI + WebSocket backend — see backend/main.py.
fastapi
uvicorn[standard]
httpx  # needed by fastapi.testclient.TestClient, not the app itself

# Google Sign-In — see backend/auth.py. google-auth verifies a Google ID
# token server-side (against Google's own public keys, no network call of
# ours needed at verify time); pyjwt issues/verifies this app's own
# longer-lived session token afterward, since a Google ID token itself
# expires in ~1 hour and isn't meant to be a standing app session.
google-auth
pyjwt
